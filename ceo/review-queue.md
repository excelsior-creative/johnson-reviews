# Review queue

Places Brandon has mentioned, plus places imported from the legacy
WordPress site that need a re-write or freshen-up. Status legend:

- `INTAKE` — mentioned, no info gathered yet
- `INFO_PENDING` — questions out to Brandon
- `DRAFTING` — being written
- `REVIEW` — draft sent to Brandon, awaiting redline
- `APPROVED` — Brandon signed off, ready to publish
- `PUBLISHED` — live on site
- `LEGACY_OK` — already migrated from WordPress, may eventually
  warrant a refresh but not blocking

## Active queue

| # | Place | Status | Notes / questions out |
|---|-------|--------|----------------------|
| — | (no fresh trips queued yet) | — | Awaiting Brandon to surface backlog |

## Legacy backlog (from `reference/`)

The WordPress migration brought ~80 published reviews into the
Reviews collection (see commits `b170b2c`, `85e40a4`, `0176d77`).
Spot-check pass needed:
- Are slugs clean and consistent?
- Are photos inline + alt-text present?
- Is the body lexical-formatted correctly?
- Does the voice still sound like Brandon, or is some of it WP-era
  AI-flavored prose?

Plan: sample 5 random legacy reviews each night, log findings here
under "Legacy refresh notes," propose any rewrites as DRAFTING items
above. Do not silently rewrite published reviews — Brandon must
approve each refresh.

## Legacy refresh notes

(empty — start filling on next run)

## Standing prompts for Brandon

When the queue thins out, ping Brandon for:
1. The next 1–3 trips/meals to capture.
2. Whether to start the legacy-review refresh pass (Y/N).
3. Whether any specific Google Photos album to start with.
