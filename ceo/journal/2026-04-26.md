# Journal — 2026-04-26 (Sunday night / early morning)

**Second nightly run.** First run was 2026-04-20; six nights since then.

---

## Arrival State

- Branch `main` clean and up to date.
- Most recent commit: `3d2f83f Add CEO daily instructions` (just the CEO instruction doc addition — no code).
- Live site `johnsonreviews.com` **is accessible** — resolved! (First run had 403/ECONNREFUSED; tonight it loaded without issue from the host browser.)
- Live site running the PR #15 build (`design-system-update-n1v5r` from around 2026-04-20). This means the PR #12 copy rewrites (Hero, About, Contact, etc.) are NOT yet live on production — those changes are in `main` but Vercel appears to be running an older build or there's a caching issue.
- Local working tree had 7 uncommitted changed files — all carryover from work done between runs that wasn't committed. Committed + PR'd tonight as PR #19.

## Live Site Audit (what I saw tonight)

**The good:**
- Site loads. Dark gold aesthetic is live. Navbar + footer functional.
- Blog posts are rendering — Jamba Juice, Grand Californian, Hotel Mousai, etc. pulling from Payload.
- Category grid present. Search dialog probably works.
- "JOHNSON REVIEWS" wordmark top-left.

**Issues spotted:**

1. **"FEATURED CRITIQUE" badge** — appears in the hero area. This string does NOT exist in the current codebase files. It must be coming from the CMS (Payload CMS, a Page's Hero block field, or a published post flagged as "featured critique"). This needs investigation.

2. **Hero content looks wrong on live** — the live site shows "EACH DAY AS A NEW DESTINATION" and a quote "Elevating the standard of taste through rigorous analysis and an uncompromising palate — every experience deserves to be remembered." This is off-brand luxury-critic copy. But checking the local codebase, `Hero.tsx` already has the correct on-brand copy ("Real visits. Honest reviews."). The disconnect: the live site is running an older build. **The copy rewrites from PR #12 (merged weeks ago) are NOT live.** Either Vercel isn't deploying from `main` properly, or there's a cache serving stale build.

3. **QuickStats shows zeros** — "Level 0+, 0+ photo views, 0+ Local Guide points." The QuickStats component is probably hardcoded with placeholder values or can't fetch real data. Harmless for now but looks bad.

4. **"AS SEEN IN" logos** — loading from sg-host CDN (old WordPress hosting). These are external dependencies. If sg-host goes down, logos disappear.

5. **"EDITOR'S CHOICE" / "FEATURED REVIEWS" labels** — still showing old wording on live. Consistent with point #2 (stale Vercel build).

## What I Shipped Tonight

### PR #19 — E-E-A-T schema, metadata fix, Hero/CategoryGrid polish

**Committed** and **pushed** the 7 uncommitted files that were sitting in working tree:

**structured-data.ts:**
- `generatePersonSchema()` — Brandon Johnson as Person entity, E-E-A-T anchor for Google
- `generateRestaurantReviewSchema()` — Restaurant + Review schema for use on individual review posts
- `generateBreadcrumbSchema()` — BreadcrumbList for post pages
- `generateArticleSchema()` — fixed author to reference Person `@id` instead of Organization
- `generateOrganizationSchema()` — added `founder` link to Person schema
- Image URL fix: handles both Payload-relative paths and external sg-host absolute URLs
- `generateGlobalSchema()` now outputs Person + Org + WebSite on every page

**metadata.ts:**
- `SITE_NAME` fixed from literal `"Template Site"` to `"Johnson Reviews"`
- `SITE_TAGLINE` fixed to `"Real Visits. Honest Reviews."`
- `DEFAULT_DESCRIPTION` now describes the actual site with Brandon's credentials
- `keywords` rewritten to real SEO targets

**layout.tsx:**
- Global schema (Person + Org + WebSite) injected into `<head>` on every frontend page

**blog/[slug]/page.tsx:**
- Article + BreadcrumbList schema injected on each post page

**Hero.tsx:**
- Removed fake 9.4/10 score disc ("The Verdict") from Hero. CTAs are now clean.

**CategoryGrid.tsx:**
- Removed fake category counts (180+, 60+, 120+, 40+). Label changed to `Browse →`.

**page.tsx (home):**
- Removed `"Seven years. Five hundred reviews."` — can't verify.

### /ceo additions

- **`ceo/prompts/intake.md`** — v1.0 review intake interview script. Complete with Step 1 (confirm basics), Step 2 (venue-type follow-ups), Step 3 (photo check), Step 4 (when to draft). Defines explicitly what we never infer vs. what's OK to infer.

- **`ceo/decisions/0001-legacy-review-migration-policy.md`** — proposed policy on legacy WP review migration. Key finding: the old reviews were written by a ghostwriter ("Hanns Bida Camay"), not Brandon. They're full of invented-feeling details and generic travel-blog language. This is a brand risk. Recommending we verify each review with Brandon before migrating, not auto-publish.

## What I Did NOT Do Tonight

- Did not migrate any legacy reviews. Blocked pending Brandon's confirmation that (a) the facts in the old ghostwritten posts are accurate and (b) he's OK with me rewriting them in his voice.
- Did not investigate the Vercel deployment issue that's causing the stale build on production. This needs Brandon to check the Vercel dashboard — it's possible auto-deploy is disabled or the wrong branch is configured.
- Did not audit QuickStats for how to show real data (Brandon's actual Google Local Guide numbers). Needs API access or manual values.
- Did not fix the "FEATURED CRITIQUE" CMS mystery — needs access to the Payload admin to see what field is generating that string.
- Did not audit the `CalculatorSection` / `ServicesSection` dead code.

## Key Finding: Live Site is Running a Stale Build

The biggest issue from tonight's audit: the live site is not running the code that's in `main`. PR #12 (copy rewrites from first nightly run) merged, but the live site still shows the old luxury-critic copy. This is a real problem — every PR we open deploys to a preview URL but possibly not to production automatically.

**Action required from Brandon:** Check the Vercel dashboard. Is auto-deploy from `main` enabled? Is the production deployment pointed at main? The Vercel environment may still be set to manual promotion.

## Asks for Brandon (batched)

See INBOX.md updates. Tonight's new asks:

- 🔴 **Vercel is not deploying from main** — the copy rewrites from weeks ago are not live. Please check your Vercel dashboard.
- 🟡 **Legacy review ghostwriter issue** — the 60+ old reviews were written by a hired ghostwriter (not you). I need your direction before migrating any of them. See decision 0001 in /ceo/decisions/.
- 🟡 **Apizza Doho / Dana Point cluster verify** — do you actually remember going to these places? The old review mentions "a group of friends before a concert on the beach" — is that accurate?
- 🟡 **"FEATURED CRITIQUE" mystery** — something in your Payload CMS is generating this label on the live home page. Probably a Page's Hero block configuration field. Can you check the Payload admin (/admin)?
- 🟢 **QuickStats real data** — your Google Local Guide stats (level, review count, photo views, points) could be pulled manually and hardcoded. What are your current numbers?

## What's Next (tomorrow night)

1. Investigate and fix the Vercel deployment issue if Brandon provides Vercel access.
2. Fix QuickStats with real Brandon credential numbers (if Brandon provides them).
3. Build the Author bio block on Post pages (E-E-A-T KR2).
4. Start one legacy review migration IF Brandon has confirmed the Apizza Doho facts.
5. Audit `CalculatorSection` / `ServicesSection` for removal.
6. Investigate the "FEATURED CRITIQUE" CMS field.
