# 2026-04-17 — first CEO night

## Arrival state

- Branch: `claude/affectionate-goodall-U6bnx` (per nightly directive).
- Working tree clean. Last commit on `main`: `e956a55 chore: add
  design` (Brandon dropped the redesign reference yesterday).
- No `/ceo` folder existed. This is night one.
- Reference design: `reference/stitch_johnson_reviews_discovery/`
  with four artifacts (gilded_legacy DESIGN.md + home + discovery +
  review-detail HTML/screen pairs).
- Site already has a substantial dark-luxury overhaul in place
  from `ef6a87a feat: dark luxury editorial aesthetic overhaul` —
  Tailwind 4 tokens, fonts, hero, navbar, footer, review card all
  on the new palette.

## What I did

### Bootstrapped `/ceo`

Full folder per the nightly spec:

- README, strategy, voice, design-system, workflow, review-queue,
  okrs, roadmap, backlog, photos, seo, metrics, competitive,
  experiments, INBOX
- `decisions/0001-keep-stitch-redesign-as-source-of-truth.md`
- `journal/2026-04-17.md` (this file)
- `content-drafts/` placeholder

Big call I made tonight without sign-off: **design-system.md**
captures the Stitch reference as the source of truth for visual
direction, but explicitly carves out *copy* — the reference's
fine-dining-critic placeholder text ("rigorous analysis,"
"uncompromising palate," "About the Critic") is **not** part of the
brand voice. Logged that as a question in INBOX.md so Brandon can
override.

### Shipped: legacy palette purge

Sticking out badly across the site were leftover bits from the old
WordPress aesthetic: orange `#DB7D2D` accents, `Jost` body font,
`Oswald` headings, `Unna` date pills. Migrated to the dark-luxury
gold + Noto Serif + Inter system in:

- `apps/app/src/components/SubscribeForm.tsx` — orange Subscribe
  button + Jost input → ghost-bordered Inter input + gilded-gradient
  CTA.
- `apps/app/src/components/LexicalContent.tsx` — orange list markers
  and blockquote borders → gold; added `prose-headings:font-headline`
  + champagne italic blockquotes + gold underline links.
- `apps/app/src/app/(frontend)/reviews/page.tsx` — full rewrite of
  the listing page: editorial kicker + serif title + champagne deck +
  Inter caps filter pills with a gilded-gradient active state. Star
  filters now show actual gold-SVG stars instead of emoji.
- `apps/app/src/app/(frontend)/reviews/[slug]/page.tsx` — full
  rewrite of the detail header: kicker for address, serif title,
  Inter caps date, gold star rating, gold-divider underline; image
  gallery uses the grayscale-on-rest-color-on-hover treatment from
  the design system.

After: `grep -E "DB7D2D|Jost|Oswald|Unna" apps/app/src` → no matches.

`pnpm type-check` cleared my files. Pre-existing errors remain
(`@/payload-types` is gitignored intentionally; `sitemap.ts` had a
pre-existing TS error before tonight).

## What I did NOT do (intentionally)

- **Did not rewrite Hero / Footer copy.** The home Hero currently
  reads "EACH DAY AS A NEW DESTINATION / Elevating the standard of
  taste through rigorous analysis and an uncompromising palate"
  with a "About the Critic" CTA — that's the reference HTML's
  placeholder copy and it clashes with the brand premise. I have a
  Brandon-voice draft in mind ("Real trips. Honest reviews. Written
  by a dad who actually went.") but did not ship it. Queued in
  INBOX as 🟡 #02.
- **Did not touch the Hero or Footer code.** Same reason — both
  contain copy that needs Brandon's call before I overwrite.
- **Did not start the v1 review-intake CLI.** Bigger lift; needs a
  schema decision (single rating vs. sub-scores → INBOX 🟡 #03)
  before I write code.

## Asks for Brandon (also in INBOX.md and tonight's Slack)

1. 🟡 Pronouns + naming on reviews ("we"/"I", kids by name?, wife
   by name?).
2. 🟡 Hero / Footer copy — want a Brandon-voice rewrite proposed,
   or do you have copy in mind?
3. 🟡 Rating system — single 1–5 star, or sub-scores
   (Food/Service/Vibe/Family-Friendliness/Value)?
4. 🟢 Backlog seed — next 1–3 trips you want written up first?
   Google Photos album link is enough.
5. 🟢 Social handles — Twitter/X, Instagram, Facebook, TikTok?
6. 🟢 AsSeenIn bar — Yelp / OpenTable / Local Guides logos:
   actual placements or aspirational?

## Tomorrow / next session

- Once Brandon answers #02: ship the Hero + Footer voice rewrite.
- Once Brandon answers #03: either extend the Reviews schema or
  scale the Verdict block to a single rating.
- Start the reference-faithful Reviews discovery layout (sidebar
  filters + bento featured card) — see roadmap NOW item #3.
- Add JSON-LD `<script type="application/ld+json">` to review
  detail (Restaurant + Review + AggregateRating).
- Wire Vercel Analytics in `(frontend)/layout.tsx`.
- Sample 5 legacy reviews and audit voice → log to
  `review-queue.md` "Legacy refresh notes."

## North-star metric snapshot

- Reviews published net-new under CEO regime: **0**.
- Drafts in flight: **0**.
- Time `/ceo` exists: **night 1**.

Tomorrow this section gets numbers.
