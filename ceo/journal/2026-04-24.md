# Journal — 2026-04-24 (Friday night)

Second nightly run. Four nights since the first run (2026-04-20).

## Arrival state

- Branch: `claude/relaxed-hamilton-dEc8A` (clean tree at start).
- **Big news:** PR #15 merged 2026-04-23 — Timmy at Excelsior
  Creative shipped a **new dark editorial design system** (Johnson & Co.)
  from `reference/design2/`, replacing the Gilded Legacy palette. The
  whole site was rebuilt on the new tokens: home, reviews list/detail,
  blog list/detail, about, contact, privacy, terms, plus new shared
  components (Wordmark, ScoreDisc, PageHeader, NewsletterInline).
- Open INBOX: 10 items from 2026-04-20 still awaiting Brandon.
- The voice copy pass from 2026-04-20 (PR #12) is live — but some of
  the pages Timmy rebuilt may need a voice re-audit. Not tonight;
  flagged for next run.

## What I shipped tonight

### 🔴 Brand-critical fix: killed "Template Site"

The biggest single-leverage bug in the codebase — `SITE_NAME =
"Template Site"`, `SITE_TAGLINE = "Built with Next.js & Payload CMS"`,
keywords `["Next.js","Payload CMS","React"…]` in `apps/app/src/lib/metadata.ts`.
These flowed into:

- Organization JSON-LD embedded on every page (Google saw "Template Site")
- WebSite JSON-LD
- OG card siteName
- Twitter siteName
- RSS feed channel title
- Payload SEO plugin fallback titles
- Contact email footer copyright

Rewrote `metadata.ts` with proper Johnson Reviews values, plus an
`absoluteUrl()` helper (which fixes a latent bug where absolute image
URLs were being double-prefixed with SITE_URL in structured data).

### 📈 SEO + structured data: full schema coverage

Rewrote `apps/app/src/lib/structured-data.ts` to ship:

- **Person schema for Brandon** (`@id = /#brandon`). E-E-A-T signal —
  `jobTitle`, `knowsAbout`, `sameAs`, `worksFor` → Organization. Added
  to the global graph so every page has Brandon-as-author in its graph.
- **Organization** updated to point at Brandon as founder.
- **Article (BlogPosting)** updated: `author` is now Brandon (Person)
  instead of the Organization. Fixed `image` URL absolution.
- **New: Review schema** for Reviews-collection docs —
  `itemReviewed` is Restaurant / LodgingBusiness / TouristAttraction
  / LocalBusiness based on category, `reviewRating` with 10-point
  scale, author Brandon, publisher Johnson Reviews.
- **New: categoryToSchemaType()** maps free-form review categories
  to the right Schema.org place type.
- **New: BreadcrumbList helper** for reviews + blog detail pages.

Wired schema into the pages:

- `apps/app/src/app/(frontend)/blog/[slug]/page.tsx` — now exports
  `generateMetadata` (title, description, OG, canonical, Article
  OpenGraph type, keywords from post tags), embeds combined
  `BlogPosting + BreadcrumbList` JSON-LD in the article footer,
  refactored data fetch into `fetchPostBySlug()` so metadata + page
  share the same Payload call.
- `apps/app/src/app/(frontend)/reviews/[slug]/page.tsx` — same
  treatment. `generateMetadata` (with city-extracted title and
  reviewText-derived description), embeds combined
  `Review + BreadcrumbList` JSON-LD.

Impact: we now serve Google a proper knowledge graph — Brandon as
Person, Johnson Reviews as Organization, each review as Schema.org
`Review` with `itemReviewed` typed to Restaurant/Lodging/etc. This
is the foundation for KR3.1 (100% schema coverage) and moves
E-E-A-T signals from near-zero to legitimate.

### 🧹 Deleted dead scaffold components

Confirmed unused (no imports): removed
`CalculatorSection`, `ServicesSection`, `AboutSection`, `CTASection`,
`Header` (component — Payload's `globals/Header` is untouched),
`Logo` (Navbar has its own wordmark). These were leftover from the
Next.js + Payload scaffold the repo was forked from (the Calculator
was a commercial real-estate depreciation widget; ServicesSection
was web-dev-agency copy). Closes INBOX #9.

### 🧾 Strings

- Contact email footer: `© Template Site` → `© Johnson Reviews`.
- `payload.config.ts` SEO plugin fallbacks:
  `"Your Site"` → `"Johnson Reviews"`.

### 📝 /ceo updates

- **`design-system.md` fully rewritten** for the new Johnson & Co.
  dark editorial system. Captures the new color tokens (#0f0d0b base,
  #c9a961 accent), the four-face type system (Playfair + Inter +
  JetBrains Mono + Caveat), component primitives (.btn, .score-disc,
  .tag with gold ::before rule, .signature cursive), the shipped page
  status, and the gaps still to fill (empty / 404 / loading /
  category hubs / location hubs / tag pages / pagination). Old
  Gilded Legacy doc superseded, archived in git history.
- **`prompts/intake.md` written** — full conversational review intake
  script. Two-pass interview (3 opener questions + logistics
  follow-up), drafting rules, photo proposal rules (geotag scrubbing
  for kid photos), Brandon handoff format, publish steps, a
  "legacy-migration variant" for the 60+ WP reviews in the queue, and
  a hard "things the agent must never do" list. Workflow.md will
  cite this as the canonical intake doc.

## Typecheck

Ran `pnpm run type-check` after the edits. Clean — no TS errors. Had
to regenerate `payload-types.ts` first since it's gitignored.

## What I did NOT do tonight

- **No legacy review migration.** Still blocked on INBOX #1 (live
  site reachability), #7 (approve-by-silence policy for legacy
  migrations), and I don't want to publish reviews Brandon hasn't
  signed off on. Once Brandon answers #7 I can start the Dana Point
  cluster.
- **No voice re-audit of the new Timmy pages.** PR #15 rebuilt
  home/about/contact/privacy/terms on new components — need to
  re-check that the voice pass from 2026-04-20 still reads right on
  these rebuilt pages. Flagged for next run.
- **No mobile audit.** KR1.4 still ⬜.
- **No empty / 404 / loading states.** KR1.3 still ⬜.
- **No Author bio block** as a standalone component — the inline
  BJ avatar + name is already baked into the new review/blog
  templates (Timmy shipped it in PR #15). Promoting it to a
  `<AuthorBio>` with proper Person schema isn't needed now that the
  Person is in the global graph.
- **No live deploy verification.** Still INBOX #1.

## Asks for Brandon (net-new tonight)

Only one new ask tonight. The other 10 from 2026-04-20 are still open.

11. 🟢 **Dead scaffold components removed** — deleted six unused
    components (`CalculatorSection`, `ServicesSection`,
    `AboutSection`, `CTASection`, `Header`, `Logo`). These were WP/
    agency-scaffold leftovers. If you actually wanted any of them,
    yell. Otherwise this is FYI.

## Cycle time

- Run start → run finish: ~1h in-session.
- No review draft cycles tonight.

## What's next (next nightly run, in order)

1. **Voice re-audit** of the pages Timmy rebuilt in PR #15
   (home hero, about, contact, privacy, terms). Make sure no
   luxury-critic placeholder copy slipped back in.
2. **Location hub: Dana Point proof of concept** — build
   `/location/[slug]/page.tsx` as a server component, seed a
   Locations collection or just use slug-as-config, render a
   PageHeader + list of reviews filtered to that city. Dana Point
   first (highest density).
3. **Sitemap audit** — fix the `sitemap.ts` TS error I saw
   (unrelated to my edits, but it was an unused `any` narrowing).
   Verify the output includes all Posts + Reviews + static pages.
4. **Empty / 404 / loading states** — design in-spirit of the
   editorial system.
5. **Pick the first legacy migration** once Brandon has answered
   INBOX #7.

## PR for tonight's work

Opening as draft from branch `claude/relaxed-hamilton-dEc8A`.
