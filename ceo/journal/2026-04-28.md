# Journal — 2026-04-28

## Nightly CEO routine

### Arrival state

- Main worktree was dirty on `docs/2026-04-27-peter-move-forward` with local edits to `ceo/INBOX.md` and `ceo/journal/2026-04-27.md`, and the branch was both ahead and behind its remote.
- To avoid mixing scheduled-job state with local artifacts, I fetched `origin` and created an isolated worktree from `origin/main` on `docs/2026-04-28-nightly-ceo`.
- `origin/main` is at `462cd78` (`docs: log empty ready queue for nightly run (#21)`).

### Context reviewed

- `ceo/config.md` — default risk tier is R2; auth/PII/moderation/monetization raise to R4.
- `ceo/conventions.md` — branches and PRs only; work from `ceo/specs/`; journal every run; blockers in `ceo/INBOX.md`.
- `ceo/next.md` — no ready specs are queued on `main`; agents must not invent work when fewer than two ready items exist.
- `ceo/README.md`, `ceo/daily-instructions.md`, `ceo/roadmap.md`, `ceo/backlog.md`, `ceo/review-queue.md`, `ceo/okrs.md`, `ceo/INBOX.md`, and the latest journal.
- `ceo/specs/` — no executable specs exist on `main` yet.
- Open PR queue via GitHub CLI.

### Readiness finding

`main` is still blocked by the BOS ready-work guardrail: `ceo/next.md` has zero ready specs and `ceo/specs/` is empty. I did not implement roadmap/backlog/code work directly.

There is now a mergeable planning PR that appears intended to unblock this:

- PR #22 — `docs: queue Johnson Reviews next specs` (`docs/2026-04-27-peter-move-forward`)
- It adds three scoped specs, completes the review intake template pack, and queues two ready items: Author Bio Block and Sitemap + Schema Coverage Audit.
- Risk/approval in PR body: R2 documentation/planning; Brandon asked Peter in Slack thread `C0AHR2NJ361 / 1777339258.868489` to move the project forward.

### Production / CI finding

The PR #22 Vercel check is failing before merge, even though the PR is documentation/planning only. I inspected the Vercel deployment logs for `dpl_HhY6gwMFkx2kLDEL77afdVo6bBRQ`; the failing build error is pre-existing app code/config state:

```text
./src/app/(frontend)/blog/[slug]/page.tsx:9:44
Type error: Cannot find module '@/payload-types' or its corresponding type declarations.
```

Supporting repo facts:

- `apps/app/src/payload.config.ts` is configured to generate `apps/app/src/payload-types.ts`.
- `ceo/roadmap.md` records PR #7 as `Remove payload-types from tracking`.
- No `payload-types.ts` file exists in the checkout.
- Multiple app files still import `@/payload-types`.

This likely means Vercel/builds need a reliable type-generation step or a committed/generated type strategy. Because this is code/build pipeline work and no ready spec exists on `main`, I logged it as a blocker rather than patching it ad hoc.

### Live-site check

- `https://johnsonreviews.com` returned HTTP 200 from this environment.
- `https://www.johnsonreviews.com` failed DNS resolution from this environment.
- The apex page no longer contained the previously flagged strings `EACH DAY AS A NEW DESTINATION`, `Established 1984`, or `Sommelier`, and did contain `Local Guide` / `Family` language.
- The rendered page still preloads assets from `brandonj117.sg-host.com`, matching the existing legacy-image dependency FYI in `ceo/INBOX.md`.

### Work performed

- Added this journal entry.
- Added a new urgent `ceo/INBOX.md` item for the Vercel/build failure caused by missing `@/payload-types`.
- Did not modify `ceo/next.md`; it remains authoritative and empty on `main` until PR #22 or equivalent is merged.

### Blockers

- `main` has no ready specs in `ceo/next.md` and no files under `ceo/specs/`.
- PR #22 queues ready work but its Vercel check fails due the missing generated Payload types issue.
- Production/build pipeline issue needs a scoped spec or explicit approval before code changes.
- Legacy review migration remains blocked on Brandon confirming migration policy and facts.

### Next recommended action

1. Approve/merge PR #22 once the Vercel/build gate is understood or bypassed for docs-only PRs; it is the clean path to restore two ready specs.
2. Convert the missing `@/payload-types` build failure into an urgent ready spec so Devon can add a reliable Payload type-generation/build strategy.
3. After PR #22 lands, execute the Author Bio Block spec first; it is the highest-value public-site improvement already scoped.
