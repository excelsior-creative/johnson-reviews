# Journal — 2026-04-22 (Wednesday night)

Second nightly run. Yesterday (2026-04-21) is a skipped night — no journal
entry exists. Catching up tonight.

## Arrival state

- Branch: `claude/relaxed-hamilton-WXrQV`, clean working tree. Last
  merged work on the repo was PR #12 ("content: rewrite off-brand
  placeholder copy to Brandon's voice + bootstrap /ceo") which
  landed 2026-04-20.
- No Brandon input yet on last night's 10 INBOX asks (still all
  open). Tonight's work avoids anything that depends on those
  answers.
- /ceo intact from 2026-04-20 bootstrap.

## What I shipped tonight

### 1. Metadata + schema foundation — **critical brand bug**

`apps/app/src/lib/metadata.ts` had `SITE_NAME = "Template Site"`,
`SITE_TAGLINE = "Built with Next.js & Payload CMS"`, and a
template-y description. These feed `defaultMetadata`, the OG tags,
the Twitter cards, the RSS feed, and the contact email footer. So
every unsanitized page + RSS item + contact email was signing off
as "Template Site." **Fixed.**

- Rewrote `lib/metadata.ts` with correct `SITE_NAME = "Johnson
  Reviews"`, tagline "Real visits. Honest reviews.", an on-voice
  description, and SEO keywords grounded in Brandon's geography
  (Orange County, Dana Point, Irvine, Las Vegas, Disneyland,
  family travel). Also exports `AUTHOR_NAME`, `AUTHOR_TITLE`,
  `AUTHOR_LOCATION` for use by the Person schema.
- Rewrote `lib/structured-data.ts`:
  - Added **`generatePersonSchema`** — Brandon Johnson as the
    author entity, with `jobTitle` ("Google Local Guide, Level
    10"), Orange County location, `knowsAbout` keywords, and a
    `sameAs` to his real Google Maps contributor profile. This is
    the E-E-A-T signal.
  - Rewrote `generateArticleSchema` to attribute author to the
    Person (Brandon), not the Organization.
  - Added **`generateReviewSchema`** — emits a schema.org
    `Review` with `itemReviewed` typed based on the post's
    category slug (Restaurant / LodgingBusiness /
    TouristAttraction / LocalBusiness / Place). Conservative
    default (Place) when we can't confidently classify.
  - `generateGlobalSchema` now includes Person, Organization, and
    WebSite in a single `@graph` on every page.
- `app/layout.tsx` description rewrite: was "culinary journey …
  premier guide to discovering hidden gems" (off-brand — "hidden
  gems" is on Brandon's avoid list) → now `DEFAULT_DESCRIPTION`
  from metadata.ts.
- Wired article + review JSON-LD into `/blog/[slug]/page.tsx`
  via an inline `<script type="application/ld+json">` in the
  article. Review schema only emits when an itemName is provided
  (currently uses `post.title` — good proxy until we add a
  dedicated "place name" field).
- `api/contact/route.ts`: the transactional email template had
  the "Template Site" footer and #FF5722 orange accents (a
  different Payload-starter palette). Replaced with `SITE_NAME`
  and the brand gold `#d4af37`.

### 2. Post detail page polish + Author bio

The post sidebar still read like a luxury-critic design system
from the reference:

- "The Concierge" → **"At a glance"**.
- "Collection" → **"Category"** (the sidebar label for the post's
  category).
- "Date of Visit" → **"Published"** (on `/blog/[slug]`; left as
  "Date of Visit" on `/reviews/[slug]` where it really is the
  visit date).
- "Plan a Visit" button (gold gradient CTA linking to contact) —
  **removed**. Brandon isn't offering to plan people's visits.
- "Return to the Journal" back link → **"Back to all reviews."**
- Same "The Concierge" heading on the separate `/reviews/[slug]`
  (Google-reviews collection) page — fixed to "At a glance."

Added a new `<AuthorBio />` component (`apps/app/src/components/
AuthorBio.tsx`) that renders a Brandon card in the sidebar: gold
"BJ" monogram placeholder (until we have a portrait), name, Level
10 Local Guide credential, and an honest one-paragraph bio with a
link to `/about`. This is the E-E-A-T companion to the Person
schema. Wired in on `/blog/[slug]/page.tsx`.

### 3. Dead code removal

- Deleted `apps/app/src/components/CalculatorSection.tsx` and
  `ServicesSection.tsx`. Confirmed zero references in the repo
  outside the dead files themselves. Flagged as dead code in
  last night's INBOX (item #9). Safe removal.

### 4. SG-Host image dependency — dropped

Last night's INBOX #8 flagged that the site loaded its Hero
background and the "As Seen In" logos from the legacy WordPress
hoster `brandonj117.sg-host.com`. If that host is ever cancelled
or firewalled, the hero goes blank and the press-bar
logos 404.

- **`Hero.tsx`:** replaced the external `brandonj117.sg-host.com`
  `url()` background with a tonal radial-gradient fallback built
  from the Gilded Legacy palette. Added a
  `NEXT_PUBLIC_HERO_IMAGE_URL` env var as a pointer so Brandon can
  drop a real photo into `/public/hero-bg.jpg` or a CDN URL
  whenever he's ready — no code change needed then. The fallback
  is self-contained and on-brand.
- **`AsSeenIn.tsx`:** the component rendered four logos (Google
  Local Guides, OpenTable, Yelp, + a duplicate) under an "As Seen
  In" header. Two problems: (a) all four images were SG-Host
  URLs, and (b) "As Seen In" implies press coverage Brandon
  hasn't received — he reviews on those platforms, he isn't
  featured by them. Rewrote the strip as a text-only credentials
  bar: "Level 10 · 500+ reviews · 27,000+ photos · Orange County,
  CA" under the label "Built on a decade of public reviews." Every
  stat is independently verifiable on Brandon's Maps profile.
  Zero external image deps now.

### 5. Review intake workflow — the product

Built the first real version of the review intake pipeline
outside of "it's a nice idea":

- **`/ceo/prompts/intake.md`** — the interview prompt the agent
  uses whenever Brandon initiates (or we migrate). 18 numbered
  questions across basics / story / logistics / photos, plus the
  async variant: how to batch questions into one Slack message
  when Brandon isn't around.
- **`/ceo/prompts/publish-checklist.md`** — the pre-publish gate.
  Voice and accuracy (ban-list scrub, no invented details),
  structure (word count, excerpt, images), images (alt text,
  geotag-strip-on-kids rule), SEO + schema (article + review
  JSON-LD), publish mechanics (Payload `_status`, revalidation,
  sitemap), log (queue, voice redlines, journal).
- **First real draft**: `/ceo/drafts/apizza-doho-dana-point.md`.
  A migration-in-voice of the legacy 2021 Apizza Doho WordPress
  post. The 2021 original was riddled with Brandon's ban-list
  ("hidden gem," "nestled in," "culinary masterpiece," "must-visit
  that promises to leave you craving a return trip"). Rewrote
  from the factual skeleton in Brandon's voice, flagged every
  gap I couldn't fill as a `[?? Brandon — ...]` inline question
  (7 of them). This is the first concrete test of the workflow.
  Brandon answers the 7 questions, agent applies, draft goes to
  Payload.

Updated `/ceo/review-queue.md` and `/ceo/workflow.md` to reflect.

## What I did NOT do tonight

- Still no live typecheck / build in this environment
  (node_modules not installed). Risk: the AuthorBio import in
  `/blog/[slug]/page.tsx` could hit a path alias issue; the new
  structured-data imports are all from the existing `@/lib`
  convention so should be fine. **Brandon:** Vercel build will
  catch anything.
- Did not publish any reviews. The Apizza draft is sitting for
  answers.
- Did not audit or rebuild the `/blog` discovery page layout
  against the reference design yet — that's the next big front-
  end piece.
- Did not ship a Location hub (e.g. `/dana-point`) yet. Dependency:
  need at least 3 published Dana Point reviews first.
- Did not touch `/reviews/[slug]` beyond the "Concierge" →
  "At a glance" string fix. That page is the Google-reviews raw
  importer UI and isn't the critical path right now.
- Did not add a `place` field to the Posts collection (where the
  reviewed entity's address / phone / geo lives for better
  schema coverage). Parked as a next-candidate Payload schema
  change.

## Asks for Brandon

Two new this run (marked ⭐ in INBOX), plus the 10 from 2026-04-20
are still all open. Tonight's Slack post will re-surface the
Apizza blockers and the two new asks.

New tonight:
- 11. ⭐ 🟡 **Apizza Doho migration — 7 blocking questions** on
  the draft at `/ceo/drafts/apizza-doho-dana-point.md`. See
  question 1–7 in the draft's "Agent notes / pending facts."
- 12. ⭐ 🟢 **Google Maps contributor URL** — the Person schema in
  `lib/structured-data.ts` uses
  `https://maps.google.com/maps/contrib/113321871517390024887` as
  Brandon's `sameAs`. **Confirm this is your real profile URL.**
  If it's different, one-line correction and I'll swap.

## What's next (next nightly run, priority order)

1. **Discovery page (`/blog`) layout pass** against the reference
   design — the home is on-brand, the post detail is on-brand,
   the list view still needs the treatment.
2. **Apply Brandon's Apizza answers** (if received), finalize the
   draft, publish as the first proof of the workflow, and
   measure cycle time.
3. **Start the Dana Point location hub** (`/dana-point` or
   `/locations/dana-point`) — schema, intro paragraph, list of
   Dana Point reviews. Even with only Apizza + the legacy four
   (Chart House, Maison, Ramos House, Apizza), the hub is worth
   standing up as a template.
4. **Payload schema: add optional `place` group field to
   `Posts`** — `{ name, streetAddress, city, region, lat, lng,
   telephone, website, priceRange }`. Drive richer Review/
   Restaurant/LodgingBusiness schema. Migration is non-
   destructive (all fields optional).
5. **Author `photo` field on Users**, and wire `AuthorBio` to
   render Brandon's real portrait when provided (fall back to
   the gold monogram).
6. **Redirects plugin config**: set up 301s from legacy WP slugs
   → new short slugs as migrations publish (Apizza will be the
   first entry).

## Summary (for Slack)

Shipped: brand metadata + schema foundation (Brandon as Person +
Review itemReviewed typing), dead code removal, SG-Host image
decoupling, Author bio block on post detail, post-detail copy
polish, intake + publish-checklist workflow docs, and a first
legacy-review draft (Apizza Doho) awaiting your answers.
